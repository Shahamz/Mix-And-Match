# Install torch / torchvision built for YOUR CUDA first (https://pytorch.org/get-started/locally/), e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# then: pip install -r requirements.txt
# --vis_app also needs tkinter, a system package (e.g. sudo apt install python3-tk).
#
# Known-good versions: Python 3.10, torch 2.13, torchvision 0.28, diffusers 0.40, transformers 5.15,
# huggingface_hub 1.28, numpy 2.2, Pillow 12.2, PyMaxflow 1.3.2.

torch>=2.5                  # compiled flex attention with block masks
torchvision>=0.20
diffusers>=0.32             # DPMSolverMultistepScheduler with use_flow_sigmas
transformers>=4.56          # Gemma2Model, from_pretrained(dtype=...)
huggingface_hub>=0.26       # weight downloads
numpy>=1.26
Pillow>=10.1                # ImageFont.load_default(size=...)
PyMaxflow>=1.3.0            # alpha-expansion of the crop segmentation
